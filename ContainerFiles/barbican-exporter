# syntax = docker/dockerfile:1
# This Dockerfile uses multi-stage build to customize DEV and PROD images:
# https://docs.docker.com/develop/develop-images/multistage-build/

ARG VENV_TAG=3.13-trixie-latest
ARG GHCR_URL=ghcr.io/rackerlabs/genestack-images
FROM ${GHCR_URL}/openstack-venv:${VENV_TAG} AS dependency_build
WORKDIR /app
COPY scripts/barbican-exporter-app/requirements.txt .

RUN /var/lib/openstack/bin/pip install --no-cache-dir -r requirements.txt
RUN find / -name '*.pyc' -delete \
    && find / -name '*.pyo' -delete \
    && find / -name '__pycache__' -delete

FROM python:3.13-slim-trixie
LABEL maintainer="Rackspace"
LABEL org.opencontainers.image.name="barbican-exporter"
LABEL org.opencontainers.image.description="Barbican Exporter for Prometheus"
COPY --from=dependency_build /var/lib/openstack /var/lib/openstack
RUN export DEBIAN_FRONTEND=noninteractive \
    && apt-get update && apt-get upgrade -y \
    && apt-get install --no-install-recommends -y curl \
    && apt-get purge -y --auto-remove -o APT::AutoRemove::RecommendsImportant=false \
    && apt-get clean -y \
    && rm -rf /var/lib/apt/lists/*

# Set the environment variables for the barbican-exporter
ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PATH="/var/lib/openstack/bin:$PATH"

COPY scripts/barbican-exporter-app/barbican_exporter.py /app/

# Expose port from environment variable, default to 9100
WORKDIR /app
ARG EXPORTER_PORT=9100
ENV EXPORTER_PORT=${EXPORTER_PORT}
EXPOSE ${EXPORTER_PORT}
# Run the exporter, expecting config.yaml to be mounted
CMD ["python", "barbican_exporter.py"]
